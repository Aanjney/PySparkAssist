# API/runtime only (Docker image). For ingestion use full requirements.txt in a local venv.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
qdrant-client>=1.12.0
sentence-transformers>=3.3.0
groq>=0.11.0
pydantic-settings>=2.6.0
sse-starlette>=2.1.0
