# Copy to .env and edit.

# One-time ingest on the host before first API start:
#   uv sync --extra ml --extra ingest && uv run python -m pysparkassist.ingest run

# --- Required ---
GROQ_API_KEY=replace_with_your_groq_key
GROQ_MODEL=llama-3.3-70b-versatile
EMBEDDING_MODEL=BAAI/bge-base-en-v1.5

# --- Qdrant ---
# Host ingest/evals: publish qdrant port or use localhost
QDRANT_URL=http://localhost:6333
# Compose overrides this to http://qdrant:6333 inside the API container

# --- Paths (SQLITE_PATH, GROQ_LIMITS_PATH, RAW_DATA_PATH derive from DATA_DIR) ---
DATA_DIR=./data
# SQLITE_PATH=./data/graph.db
# GROQ_LIMITS_PATH=./data/groq_limits.json
# RAW_DATA_PATH=./data/raw

# --- Optional (omit to use defaults in config.py) ---
# GROQ_TEMPERATURE=0.3
# GROQ_MAX_TOKENS=2048
# RATE_LIMIT_RPM=20
# RELEVANCE_THRESHOLD=0.35
# OFF_TOPIC_SCORE_THRESHOLD=0.55
# RETRIEVAL_MODE=dense_entity_boost
# GROQ_LIMITS_STARTUP_PROBE=true
